# 📝 커밋 메시지 작성 가이드

Notion에 가독성 좋게 정리되도록 커밋 메시지를 작성하는 방법입니다.

## ✅ 권장 커밋 메시지 형식

```
[간단한 제목 - 한 줄로]

[상세한 설명 - 여러 줄 가능]
- 변경한 이유
- 해결한 문제
- 추가 정보
```

---

## 📋 실제 예시

### 예시 1: 새 기능 추가

```bash
git commit -m "사용자 인증 기능 추가

JWT 기반 로그인/로그아웃 기능을 구현했습니다.

변경 이유:
- 사용자별 데이터 보호 필요
- 안전한 세션 관리 요구사항

구현 내용:
- JWT 토큰 발급 및 검증
- 로그인/로그아웃 API 엔드포인트
- 인증 미들웨어 추가

다음 단계:
- 비밀번호 재설정 기능 추가 예정"
```

**Notion에 표시되는 모습:**
- 📝 **커밋 내용**: 사용자 인증 기능 추가
- 💡 **커밋 이유**: JWT 기반 로그인/로그아웃 기능을 구현했습니다... (상세 설명 전체)

---

### 예시 2: 버그 수정

```bash
git commit -m "로그인 시 세션 만료 버그 수정

문제:
- 로그인 후 30분이 지나면 자동 로그아웃되는 문제 발생
- 사용자가 작업 중 갑자기 로그아웃되어 불편

해결 방법:
- 세션 만료 시간을 30분에서 2시간으로 연장
- 자동 갱신 로직 추가로 활동 중에는 만료되지 않도록 개선

영향 범위:
- 모든 로그인 사용자에게 적용
- 기존 세션도 자동 갱신됨"
```

**Notion에 표시되는 모습:**
- 📝 **커밋 내용**: 로그인 시 세션 만료 버그 수정
- 💡 **커밋 이유**: 문제: 로그인 후 30분이 지나면... (전체 설명)

---

### 예시 3: 리팩토링

```bash
git commit -m "NotionService 코드 리팩토링

기존 코드 문제점:
- 중복된 API 호출 로직
- 에러 핸들링 일관성 부족
- 가독성 낮은 구조

개선 사항:
- 공통 함수 extractCommitData() 추가
- try-catch 패턴 통일
- JSDoc 주석 추가로 문서화 개선
- TypeScript 타입 안정성 강화

성능 개선:
- API 호출 횟수 30% 감소
- 코드 라인 수 20% 감소"
```

---

### 예시 4: 간단한 수정 (제목만)

간단한 변경은 제목만 작성해도 됩니다:

```bash
git commit -m "README.md 오타 수정"
```

**Notion에 표시되는 모습:**
- 📝 **커밋 내용**: README.md 오타 수정
- 💡 **커밋 이유**: (본문이 없어서 표시되지 않음)

---

## 🎯 실습: 실제로 커밋해보기

### 1단계: 파일 수정
```bash
# 아무 파일이나 수정
echo "# 테스트" >> test.md
git add test.md
```

### 2단계: 커밋 메시지 작성
```bash
git commit -m "테스트 파일 추가

Notion 커밋 동기화 테스트를 위한 파일입니다.

목적:
- 개선된 커밋 형식 테스트
- Notion 페이지 레이아웃 확인

다음 작업:
- 실제 프로젝트에 적용"
```

### 3단계: GitHub에 푸시
```bash
git push origin feature/notion-integration
```

### 4단계: Notion에 동기화
```bash
npx tsx test-commit-sync.ts
```

---

## 💡 커밋 메시지 작성 팁

### ✅ 좋은 예시
- **제목**: 명확하고 간결하게 (50자 이내)
- **본문**: 왜(Why), 무엇을(What), 어떻게(How) 변경했는지 설명
- **구조화**: 목록, 섹션으로 구분하여 읽기 쉽게

### ❌ 피해야 할 것
- 제목만 "수정", "변경", "업데이트" 같은 모호한 표현
- 너무 긴 제목 (한 줄에 다 들어가야 함)
- 기술적 세부사항만 나열 (이유와 맥락 부족)

---

## 📊 Notion에서 보이는 최종 결과

커밋 후 Notion 페이지 구성:

```
┌─────────────────────────────────────────┐
│ 📅 커밋 날짜: 2025년 11월 30일 오후 4:30  │
│ 👤 작성자: Seongbin                      │
├─────────────────────────────────────────┤
│ 📝 커밋 내용                             │
│ > [제목이 인용구로 강조 표시]             │
│                                         │
│ 💡 커밋 이유                             │
│ [본문 내용이 깔끔하게 정리]               │
│                                         │
│ 📁 변경된 파일                           │
│ • file1.ts                              │
│ • file2.ts                              │
│                                         │
│ 🔗 GitHub에서 보기: [링크]               │
│ SHA: abc1234                            │
└─────────────────────────────────────────┘
```

---

## 🚀 자동화 스크립트

매번 수동으로 동기화하기 귀찮다면:

```bash
# package.json에 스크립트 추가
"scripts": {
  "commit:sync": "git push && npx tsx test-commit-sync.ts"
}

# 사용법
npm run commit:sync
```
